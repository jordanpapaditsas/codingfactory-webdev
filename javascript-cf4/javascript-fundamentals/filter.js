const products = ['Orange', 'Apple', 'Milk', 'Honey'];

const filtered = products.filter(product => product === 'Apple');

console.log(filtered);



const mappedProducts = products.map(product => "Product: " + product);

console.log(mappedProducts);